import math
pi = 3.14
x = 1
y = 2
z = 3
#print(round(pi)) - rounds the number to the nearest whole integer
#print(math.ceil(pi)) - rounds the number UP to the closest integer
#print(math.floor(pi)) - rounds a number DOWN to the closes integer
#print(abs(pi)) - The abs() function returns the absolute value of the specified number.
#print(pow(pi,3)) - power raises a base number to a power
#print(math.sqrt(pi)) - returns the square root of given number
#print(max(x,z,y)) - finds the maximum number and returns it
#print(min(x,y,z)) - finds the minimum numnber and returns it